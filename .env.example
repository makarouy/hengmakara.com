# Authentication (Required)
# Use a long, random string. You can generate one with `openssl rand -base64 32`
JWT_SECRET=

# Data Persistence (Optional)
# Set to 'true' to enable writing to JSON files in data/ folder.
# Default is 'false' (ReadOnly/In-Memory mode) for Vercel/Netlify.
# On VPS/Local, set to 'true'.
ENABLE_PERSISTENCE=true

# Email Configuration (for Contact Form)
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-16-char-app-password
